# ==================== ILLUSTRATOR SERVICE CONFIGURATION ====================

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true
LOG_LEVEL=INFO

# ==================== LLM CONFIGURATION (GEMINI) ====================

# GCP Project ID (REQUIRED for Gemini API)
GCP_PROJECT_ID=your-project-id-here

# Vertex AI Configuration
GEMINI_LOCATION=us-central1

# Pyramid-specific LLM Model (REQUIRED)
# Options: gemini-2.0-flash-exp, gemini-1.5-flash, gemini-1.5-pro
LLM_PYRAMID=gemini-2.0-flash-exp

# Funnel-specific LLM Model (REQUIRED)
# Options: gemini-2.0-flash-exp, gemini-1.5-flash, gemini-1.5-pro
# Can be same or different from LLM_PYRAMID based on testing/performance
LLM_FUNNEL=gemini-2.0-flash-exp

# Concentric Circles-specific LLM Model (REQUIRED)
# Options: gemini-2.0-flash-exp, gemini-1.5-flash, gemini-1.5-pro
# Can be same or different from other illustration models based on testing/performance
LLM_CONCENTRIC_CIRCLES=gemini-2.0-flash-exp

# ==================== AUTHENTICATION ====================

# Use Application Default Credentials (ADC)
# Setup: gcloud auth application-default login
# Or: Set GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ==================== NOTES ====================

# 1. Copy this file to .env and fill in your values
# 2. Never commit .env to version control
# 3. Ensure GCP_PROJECT_ID, GEMINI_LOCATION, and all LLM_* variables are set
# 4. ADC must be configured for Vertex AI access (gcloud auth application-default login)
# 5. Each illustration type (PYRAMID, FUNNEL, CONCENTRIC_CIRCLES) can use different models
# 6. This allows per-illustration model optimization without code changes
